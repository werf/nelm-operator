apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "nelm-controller.fullname" . }}-leader-election-rolebinding
  labels:
    {{- include "nelm-controller.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "nelm-controller.fullname" . }}-leader-election-role
subjects:
- kind: ServiceAccount
  name: {{ include "nelm-controller.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
